<div class="container">
<div class="form-signup-title rounded mt-5">
  <h2>新規会員登録</h2>
</div>
  <div class="info-label rounded">
    <p>ZerowanAppをご利用頂くには会員登録が必要となります。</p>
    <p>既に会員登録をおすみの方はこちらからログインください。</p>

    <p>新規会員登録に関するご注意</p>
    …<br>
    …<br>
    …<br>
    …<br>
  <p>注意事項に同意する</p>
  </div>

  <div class="form-signup rounded mt-5">
    <%= form_with model: @user, url: users_sign_up_confirm_path, local: true do |f| %>
      <%= render 'devise/shared/error_messages', resource: resource %>

      <div class="form-label">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: 'email', placeholder: 'someone@example.com',
                                  class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :password, 'パスワード' %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %>
        <%= f.password_field :password, autocomplete: 'new-password', placeholder: 'password', class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, autocomplete: 'new-password', placeholder: 'password',
                                                     class: 'form-control' %>
      </div>

  <h3 class="mt-5">ご利用者情報</h1>
        <%= f.fields_for :profile do |profile_fields| %>
          <div class="form-label">
          <%= profile_fields.label :name %>
          <%= profile_fields.text_field :name, autocomplete: 'name', placeholder: '山田太郎', class: 'form-control' %>
          </div>

          <div class="form-label">
          <%= profile_fields.label :address %>
          <%= profile_fields.text_field :address, autocomplete: 'address', placeholder: '東京都新宿区西新宿２丁目８−１',
                                                  class: 'form-control' %>
          </div>

          <div class="form-label">
          <%= profile_fields.label :phoneNumber %>
          <%= profile_fields.text_field :phoneNumber, autocomplete: 'phone_number', placeholder: '01234567890',
                                                      class: 'form-control' %>
          </div>

          <div class="form-label">
          <%= profile_fields.label :birthday %>
          <%= profile_fields.date_field :birthday, autocomplete: 'birthday', class: 'form-control' %>
          </div>

          <div class="form-label">
          <%= profile_fields.label :breedingExperience %>
          <%= profile_fields.text_field :breedingExperience, autocomplete: 'breeding_experience',
                                                             placeholder: '犬５年、猫１年など', class: 'form-control' %>
          </div>

        <% end %>

      <div class="actions">
        <%= f.submit '登録内容確認', class: 'btn btn-lg comfirm-btn btn-block' %>

      </div>
    <% end %>

    <%= render 'devise/shared/links' %>
  </div>
</div>
