<h3>ペット情報を編集する</h3>
<div>
  <%= form_with model: @pet, url: pet_path, local: true, multipart: true do |f| %>
    <div class="form-signup rounded mt-5 mb-2">
      <%= fields_for @pet_images do |img| %>
        <div class="image_input-label text-center">
          <%= img.label :photos, '画像選択', class: 'image_input_btn' %>
          <%= img.file_field :photos, multiple: true, include_hidden: false, class: 'image_form' %>
          <span>４枚まで登録可能</span>
        </div>
      <% end %>
    </div>
    <div class="form-signup rounded mt-5">
      <div class="form-label">
        <%= f.label :category %>
        <%= f.select :category, Pet.categories_i18n.invert, {}, class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :petname %>
        <%= f.text_field :petname, class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :gender %>
        <%= f.select :gender, Pet.genders_i18n.invert, {}, class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :age %>
        <%= f.number_field :age, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :classification %>
        <%= f.select :classification, Pet.classifications_i18n.invert, {}, class: 'form-control' %>
      </div>


      <div class="form-label">
        <%= f.label :introduction %>
        <%= f.text_field :introduction, class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= f.label :vaccination %>
        <%= f.select :vaccination, Pet.vaccinations_i18n.invert, {}, class: 'form-control' %>

      </div>
      <div class="form-label">
        <%= f.label :castration %>
        <%= f.select :castration, Pet.castrations_i18n.invert, {}, class: 'form-control' %>
      </div>

      <div class="form-label">
        <%= fields_for @pet_areas do |p| %>
          <div class="field">
            <%= p.label :areas %><br/>
            <%= p.collection_check_boxes(:areas, Area.all, :id, :place_name, include_hidden: false) do |area| %>
              <%= area.label { area.check_box + area.text } %>
            <% end %>
          </div>
        <% end %>
      </div>

      <div class="actions">
        <%= f.submit 'ペット情報変更', class: 'btn btn-lg comfirm-btn btn-block' %>
      </div>
  <% end %>
  </div>
</div>